# mart.yml — manifest for the gme-options-mart canonical example.

mart_name: gme-options-mart
prefix: gme

# Phase tracking — updated by skills as they run.
# Phase C signed per orchestrator directive dd0fca3e + reviewer 33cc262a
# (Phase C.5 round-2 approve-on-one-mechanical-fix). Round-3 patch:
# gme_ads_market_dashboard latest_date CTE now gates on
# pull_ts_utc <= now() in both joined sides so future-dated TC-16/TC-17
# fixture rows (2098-2099) cannot be selected as the dashboard's
# "latest" trading date. TDD §T-14 trading_date row updated to match.
# All other C.5 round-2 findings (F1, F2, F3, M4, M5, M6) already
# structurally closed at 034bfc3. Phase D (/mart-dqc) begins from this
# signed scaffold.
phase: C_signed
scaffold_generated_at: 2026-06-02T03:50:00Z
c_signed_at: 2026-06-02T04:35:00Z

# Artifact paths (relative to this mart root, i.e. examples/gme-options-mart/).
source_catalog_path: docs/source_catalog.json
brd_path: docs/business-requirements.md
tdd_path: docs/tech-design-doc.md

# Sign-off flags.
brd_signed: true
tdd_signed: true

# Issue tracker binding (used by /linear if invoked from this mart).
issue_tracker:
  provider: github
  project: your-mart-org/your-mart-repo

# Dashboard mode toggle.
dashboard:
  mode: fixture
  port: 8503

# Data refresh cadence — daily after US market close.
refresh:
  schedule: "30 21 * * 1-5"
  sla_hours: 2

last_run: null
